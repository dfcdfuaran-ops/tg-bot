# - - - - - КОНФИГУРАЦИЯ ПРИЛОЖЕНИЯ - - - - - #

# Домен, через который бот доступен публично.
# Используется для получения вебхуков от сервисов (например, Telegram, платежные системы).
# !!! ВАЖНО: Укажите домен БЕЗ HTTP/HTTPS и БЕЗ косой черты в конце.
APP_DOMAIN=test-a.dfc-online.com

# IP адрес, на котором веб-сервер бота будет прослушивать запросы.
APP_HOST=0.0.0.0

# Номер порта для веб-сервера бота.
APP_PORT=5000

# Список поддерживаемых языков, разделённых запятой.
# Все доступные языки см.: remnashop/src/core/enums.py
# Для работы должны существовать переводы в remnashop/assets/translations.
APP_LOCALES=ru

# Язык по умолчанию, если предпочтительный язык пользователя не поддерживается.
APP_DEFAULT_LOCALE=ru

# Ключ шифрования для защиты конфиденциальных данных (например, токены платежей) в БД.
# !!! КРИТИЧЕСКИ ВАЖНО: Используйте сильный, уникальный ключ.
APP_CRYPT_KEY=JAX07RRPUepE9DXCMZUDbKHSCBGGlPH7OY22Ic9+1GM=


# - - - - - КОНФИГУРАЦИЯ БОТА - - - - - #

# Токен API Telegram бота. Получите его в https://t.me/BotFather/
# !!! КРИТИЧЕСКИ ВАЖНО: Никогда не делитесь этим токеном. Он даёт полный контроль над ботом.
BOT_TOKEN=8514788476:AAEMytKA1xsyEqY5HJq2N5kWUIL4XbcJ2-w

# Секретный токен для проверки вебхуков. Гарантирует, что запросы от Telegram.
BOT_SECRET_TOKEN=0bd9f1a81b2b2f3d7bc54e40e078d462cd4cd7ce11070fc6feefb2c9cf0f442ac4da3975cc670df97eb26aa13465f4c5671cd1c5b08776283cc82c5030ef9cf0

# ID Telegram разработчика. Даёт расширенные права.
BOT_DEV_ID=855511342

# Telegram никнейм аккаунта поддержки для deep-linking (без @).
BOT_SUPPORT_USERNAME=test3_support_bot

# Управляет поведением кнопки "Подключить":
# - пусто или false  -> открывает страницу подписки в браузер
# - true             -> открывает страницу подписки в WebApp
# - URL (например, https://example.com/) -> открывает указанный URL в WebApp
# Для использования страницы подписки разверните remnawave-subscription-page:
#   https://docs.rw/docs/install/remnawave-subscription-page
# Для использования пользовательской страницы подписки (Orion):
#   https://github.com/legiz-ru/Orion
# Для использования Mini App (Maposia):
#   https://github.com/maposia/remnawave-telegram-sub-mini-app
BOT_MINI_APP=false

# Ссылка на телеграм группу сообщества (необязательно).
# Если указана, в главном меню появится кнопка "Сообщество".
# Пример: https://t.me/your_community_group
BOT_COMMUNITY_URL=https://t.me/+SIFXZTHLwac5ZjJi

# Удалять ли вебхук бота при завершении.
# Установите 'true' для автоматического удаления вебхука.
# Установите 'false' чтобы оставить активным (бот проверит валидность при запуске).
BOT_RESET_WEBHOOK=false

# Отбрасывать ли необработанные обновления при перезапуске бота.
# Установите 'true' чтобы игнорировать старые сообщения.
BOT_DROP_PENDING_UPDATES=false

# Установить ли стандартные команды бота (/start, /help) при запуске.
BOT_SETUP_COMMANDS=true

# Использовать ли баннеры.
BOT_USE_BANNERS=true


# - - - - - КОНФИГУРАЦИЯ REMNAWAVE - - - - - #

# !!! КРИТИЧЕСКИ ВАЖНО !!!
# Для корректной работы вебхук бота должен быть настроен.
# Установите WEBHOOK_ENABLED и WEBHOOK_URL в .env файле панели Remnawave
# чтобы они указывали на ваш endpoint бота, например:
# https://bot.domain.com/api/v1/remnawave
# !!! КРИТИЧЕСКИ ВАЖНО !!!

# Имя хоста или название Docker сервиса для подключения к API Remnawave.
# !!! ВАЖНО: Укажите домен БЕЗ HTTP/HTTPS и БЕЗ косой черты в конце,
# или используйте имя Docker контейнера.
REMNAWAVE_HOST=remnawave

# Номер порта для подключения к API Remnawave.
# Требуется только если панель запущена на том же сервере в Docker.
REMNAWAVE_PORT=3000

# API токен для аутентификации с сервисом Remnawave.
# !!! КРИТИЧЕСКИ ВАЖНО: Держите этот токен в секрете, никогда не раскрывайте его публично.
REMNAWAVE_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1dWlkIjoiOTJmMDkwNGMtZTUzYy00ODg2LThlYjItZmNmZTUyNmE0YTRhIiwidXNlcm5hbWUiOm51bGwsInJvbGUiOiJBUEkiLCJpYXQiOjE3NjY5MDIyNDksImV4cCI6MTA0MDY4MTU4NDl9.WWgS8MS9ieDbBpM0hUTvyMBUZCdU-W0M6xZ1xT2ja_U

# Секретный токен для проверки входящих вебхуков от Remnawave.
# В панели Remnawave это соответствует WEBHOOK_SECRET_HEADER.
REMNAWAVE_WEBHOOK_SECRET=5cb4c0bb0c971fd5004dbc28355cba8e2e0d33a73570accf22edbe27073e5f86

# Если вы используете Remnawave за Caddy с включённой аутентификацией,
# укажите значение X-Api-Key, которое ожидает Caddy для авторизованного доступа.
# Этот токен будет включён в запросы к API Remnawave.
REMNAWAVE_CADDY_TOKEN=

# Пользовательская cookie для аутентификации с Remnawave.
# Должна быть в формате 'name=value' (например, wDpdagIh=T4yWkD8rM1oF).
REMNAWAVE_COOKIE=


# - - - - - КОНФИГУРАЦИЯ БД - - - - - #

# Имя хоста PostgreSQL базы данных.
DATABASE_HOST=remnashop-db

# Номер порта для PostgreSQL.
DATABASE_PORT=5432

# Имя PostgreSQL базы данных.
DATABASE_NAME=remnashop

# Имя пользователя для аутентификации в БД.
DATABASE_USER=remnashop

# Пароль для базы данных.
DATABASE_PASSWORD=168801ed57a038af2df1ecc0abd5bf75dfee86b08728ef88

# Включить логирование всех выполненных SQL запросов.
# Установите 'true' для отладки.
DATABASE_ECHO=false

# Включить детальное логирование поведения пула подключений SQLAlchemy.
# Установите 'true' для отладки проблем с подключением.
DATABASE_ECHO_POOL=false

# Количество постоянных подключений в пуле SQLAlchemy.
DATABASE_POOL_SIZE=30

# Максимальное количество временных подключений сверх размера пула под нагрузкой.
DATABASE_MAX_OVERFLOW=30

# Максимальное время (в секундах) ожидания свободного подключения.
DATABASE_POOL_TIMEOUT=10

# Время жизни подключения (в секундах) перед переиспользованием.
# Помогает предотвратить использование устаревших подключений.
DATABASE_POOL_RECYCLE=3600


# - - - - - КОНФИГУРАЦИЯ REDIS - - - - - #

# Имя хоста сервиса Redis.
REDIS_HOST=remnashop-redis

# Номер порта для Redis.
REDIS_PORT=6379

# Индекс БД Redis.
REDIS_NAME=0

# Пароль для Redis.
REDIS_PASSWORD=59cba229b083dfcbeef4a8a211b9904e1a66bcfb41f02bb2
