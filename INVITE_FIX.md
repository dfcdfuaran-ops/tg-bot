# Исправление отображения подписки в меню "Пригласить"

## Проблема
В меню "Пригласить" в шапке отображалась константа "У вас нет оформленной подписки." вместо динамического статуса подписки, как это было в главном меню.

## Решение
Изменены два файла:

### 1. `/opt/remnashop/assets/translations/ru/messages.ftl` (строки 533-546)
**Было:**
```
<b>📋 Ваша подписка:</b>
<blockquote>
{ $has_subscription ->
[true]
• <b>Тариф:</b> { $current_plan_name }
• <b>Лимит трафика</b>: { $traffic_limit }
• <b>Лимит устройств</b>: { $device_limit }
• <b>Осталось</b>: { $expire_time }
*[false]
• У вас нет оформленной подписки.
}
</blockquote>
```

**Стало:**
```
{ hdr-subscription }{ frg-subscription-status-full }
```

Теперь используется фрагмент `frg-subscription-status-full` из `utils.ftl`, который динамически обрабатывает все статусы подписки (ACTIVE, EXPIRED, LIMITED, DISABLED, NONE).

### 2. `/opt/remnashop/src/bot/routers/menu/getters.py` (строки 305-328)
**Было:**
```python
if has_subscription:
    subscription_data = {
        "has_subscription": True,
        "current_plan_name": subscription.plan.name,
        ...
    }
else:
    subscription_data = {
        "has_subscription": False,
    }
```

**Стало:**
```python
if subscription:
    subscription_data = {
        "status": subscription.get_status,
        "current_plan_name": subscription.plan.name,
        "is_trial": subscription.is_trial,
        "traffic_strategy": subscription.traffic_limit_strategy,
        "reset_time": subscription.get_expire_time,
        ...
    }
else:
    subscription_data = {
        "status": None,
        "is_trial": False,
    }
```

Теперь `invite_getter` передаёт все необходимые переменные для полной поддержки фрагмента `frg-subscription-status-full`.

## Результат
✅ В меню "Пригласить" теперь отображается динамический статус подписки (Активна, Истекла, Исчерпан трафик, Отключена, Нет подписки) с соответствующей информацией, как и в главном меню.

✅ Одинаковый формат отображения подписки во всех местах приложения.
