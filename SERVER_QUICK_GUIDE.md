# 🖥️ Установка на сервер - Чит-лист

## Быстрый старт (30 секунд)

```bash
# 1. Клонируем проект
git clone https://github.com/DFTeams/remna-tg-bot.git tg-bot
cd tg-bot

# 2. Запускаем production установку
./install.sh --prod

# Готово! Бот запущен в production режиме
```

## Что происходит при установке

✅ Создаётся файл `.env` с параметрами  
✅ Запускаются все необходимые контейнеры (БД, Redis, Bot)  
✅ Нет папки `src` на сервере (находится в образе)  
✅ Минимум файлов на сервере (~10 MB вместо 500 MB)  

## 📂 Что остаётся после установки

```
tg-bot/
├── .env                              # Конфиг
├── docker-compose.production.yml    # Production конфиг
├── assets/                          # Ассеты
├── logs/                            # Логи
└── backups/                         # Бэкапы БД
```

## 🔧 Полезные команды

```bash
# Проверить статус
docker compose -f docker-compose.production.yml ps

# Посмотреть логи
docker compose -f docker-compose.production.yml logs -f remnashop

# Перезапустить бота
docker compose -f docker-compose.production.yml restart remnashop

# Остановить все
docker compose -f docker-compose.production.yml down

# Обновить бота (новая версия)
docker compose -f docker-compose.production.yml pull
docker compose -f docker-compose.production.yml up -d
```

## 🆘 Решение проблем

### Бот не запустился

```bash
# Проверьте логи
docker compose -f docker-compose.production.yml logs remnashop

# Проверьте переменные в .env
cat .env | grep BOT_TOKEN
```

### Нужно отредактировать конфиг

```bash
# Отредактируйте .env
nano .env

# Перезапустите
docker compose -f docker-compose.production.yml restart remnashop
```

### Нужно очистить всё и начать заново

```bash
# Остановить и удалить всё
docker compose -f docker-compose.production.yml down -v

# Удалить образ
docker rmi ghcr.io/dfteams/remna-tg-bot:latest

# Запустить заново
./install.sh --prod
```

## 📚 Документация

- 📖 [Подробная инструкция](DEPLOYMENT.md)
- 🔧 [Конфигурация](README.md)
- 💾 [Бэкапы БД](BACKUP_RESTORE_GUIDE.md)

## ❓ Частые вопросы

**Q: Почему нет папки src?**  
A: Код находится в Docker образе. На сервере только конфиг и данные.

**Q: Как обновить код?**  
A: `docker compose -f docker-compose.production.yml pull` скачает новый образ.

**Q: Где хранятся бэкапы БД?**  
A: В папке `backups/`. Их можно скопировать для безопасности.

**Q: Как запустить в режиме разработки?**  
A: `./install.sh` (без флага --prod)
