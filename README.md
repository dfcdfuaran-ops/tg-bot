# 🤖 Remnashop Bot

Telegram бот для управления подписками и продажи доступа к панели Remnawave.

## 📦 Быстрая установка

### 1. Клонирование проекта

```bash
git clone <repo_url> /opt/remnashop
cd /opt/remnashop
```

### 2. Создание .env файла

```bash
cp .env.example .env
```

### 3. Запуск автоматической настройки

```bash
./scripts/setup.sh
```

Скрипт автоматически:
- ✅ Сгенерирует ключи безопасности
- ✅ Создаст пароли для БД и Redis
- ✅ Запросит необходимые параметры
- ✅ Настроит webhook для Remnawave
- ✅ Добавит конфигурацию в Caddy/Nginx

### 4. Запуск бота

```bash
docker compose up -d
```

### 5. Проверка работы

```bash
docker compose logs -f remnashop
```

---

## ⚙️ Обязательные параметры

При запуске `setup.sh` нужно будет указать:

| Параметр | Описание | Где получить |
|----------|----------|--------------|
| **APP_DOMAIN** | Домен для доступа к боту | Ваш домен (например: `bot.example.com`) |
| **BOT_TOKEN** | Токен бота | [@BotFather](https://t.me/BotFather) |
| **BOT_DEV_ID** | Ваш Telegram ID | [@userinfobot](https://t.me/userinfobot) |
| **BOT_SUPPORT_USERNAME** | Username поддержки | Ваш username без `@` |
| **REMNAWAVE_TOKEN** | API токен | Панель Remnawave → Settings → API |
| **REMNAWAVE_WEBHOOK_SECRET** | Секрет webhook | Должен совпадать с `WEBHOOK_SECRET_HEADER` в панели |

---

## 🔧 Команды Makefile

```bash
# Запуск
make up          # Запустить все сервисы
make down        # Остановить все сервисы
make restart     # Перезапустить все сервисы

# Логи
make logs        # Посмотреть логи всех сервисов
make logs-bot    # Посмотреть логи бота

# Резервное копирование
make backup      # Создать бэкап базы данных
make restore     # Восстановить из бэкапа

# Разработка
make build       # Пересобрать образ
make shell       # Подключиться к контейнеру
```

---

## 📁 Структура проекта

```
remnashop/
├── .env                    # Конфигурация (не коммитится)
├── docker-compose.yml      # Docker Compose конфигурация
├── Dockerfile              # Dockerfile для бота
├── Makefile                # Команды для управления
├── pyproject.toml          # Python зависимости
├── uv.lock                 # Версии зависимостей
│
├── src/                    # Исходный код бота
│   ├── api/               # FastAPI endpoints
│   ├── bot/               # Telegram bot (aiogram)
│   ├── core/              # Ядро приложения
│   ├── infrastructure/    # Инфраструктурный слой
│   └── services/          # Бизнес-логика
│
├── scripts/               # Скрипты
│   ├── setup.sh          # Автоматическая настройка
│   ├── backup_db.sh      # Резервное копирование
│   ├── restore_db.sh     # Восстановление БД
│   ├── docker-entrypoint.sh
│   ├── docker-entrypoint-worker.sh
│   └── docker-entrypoint-scheduler.sh
│
├── assets/               # Ресурсы бота
│   ├── banners/         # Баннеры
│   └── translations/    # Переводы (i18n)
│
├── logs/                # Логи (монтируется в контейнер)
└── backups/             # Бэкапы БД (монтируется в контейнер)
```

---

## 🔗 Полезные ссылки

- [Документация Remnawave](https://github.com/remnawave/backend)
- [Документация aiogram](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

## 📝 Лицензия

MIT
