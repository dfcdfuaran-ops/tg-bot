#!/bin/bash
# 🚀 Быстрая установка на сервер
# 
# Использование:
#   chmod +x server-setup.sh
#   ./server-setup.sh
#
# Этот скрипт создаст .env и запустит бота в production режиме

echo "=========================================="
echo "  🚀 Установка Remnashop на сервер"
echo "=========================================="
echo ""

# Проверяем, что скрипт запущен в корне проекта
if [ ! -f "docker-compose.production.yml" ]; then
    echo "❌ Ошибка: Не найден docker-compose.production.yml"
    echo "Запустите этот скрипт из папки проекта"
    exit 1
fi

echo "✅ Обнаружен production конфиг"
echo ""

# Вызываем install.sh с флагом --prod
./install.sh --prod
